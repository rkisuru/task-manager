apiVersion: v1
kind: Service
metadata:
  name: postgres-user
  namespace: {{ .Values.namespace }}
  labels:
    app: postgres-user
    {{- include "taskflow.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    app: postgres-user
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-user
  namespace: {{ .Values.namespace }}
  labels:
    app: postgres-user
    {{- include "taskflow.labels" . | nindent 4 }}
spec:
  serviceName: postgres-user
  replicas: 1
  selector:
    matchLabels:
      app: postgres-user
  template:
    metadata:
      labels:
        app: postgres-user
    spec:
      containers:
        - name: postgres
          image: {{ .Values.images.postgres }}
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: {{ .Values.database.userDb.name | quote }}
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: taskflow-secrets
                  key: DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: taskflow-secrets
                  key: DB_PASSWORD
          volumeMounts:
            - name: postgres-user-storage
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
          resources:
            requests:
              {{- toYaml .Values.resources.postgres.requests | nindent 14 }}
            limits:
              {{- toYaml .Values.resources.postgres.limits | nindent 14 }}
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 15
            periodSeconds: 20
  volumeClaimTemplates:
    - metadata:
        name: postgres-user-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.resources.postgres.storageSize }}
